{
  "name": "backend",
  "type": "module",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "leader": "cross-env NODE_ENV=leader nodemon src/server.js",
    "follower1": "cross-env NODE_ENV=follower1 nodemon src/server.js",
    "follower2": "cross-env NODE_ENV=follower2 nodemon src/server.js",
    "follower3": "cross-env NODE_ENV=follower3 nodemon src/server.js",
    "start": "concurrently --names \"N0,N1,N2,N3\" --prefix-colors \"green,cyan,yellow,magenta\" --handle-input \"npm run leader\" \"npm run follower1\" \"npm run follower2\" \"npm run follower3\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "cross-env": "^10.1.0",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "mongoose": "^9.2.4"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "nodemon": "^3.1.14"
  }
}
